services:
  - type: web
    name: bakery-studio
    env: python
    pythonVersion: 3.11.9
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python3 -m uvicorn ui:app --host 0.0.0.0 --port $PORT
